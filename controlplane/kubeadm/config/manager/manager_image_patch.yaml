apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: quay.io/dongsupark/kubeadm-control-plane-controller:v0.3.6
          name: manager
