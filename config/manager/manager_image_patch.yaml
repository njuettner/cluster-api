apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - image: quay.io/dongsupark/cluster-api-controller-amd64:dev2021
        name: manager
